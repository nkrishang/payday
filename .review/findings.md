# Review findings — feat/attribution-kyc (git diff main...HEAD)

Last updated: 2026-09-02T13:44:18Z

Progress: 49 of 49 verified: 37 confirmed, 8 plausible, 4 rejected

Find-and-verify only; nothing has been fixed. Status values: pending / verifying / CONFIRMED / PLAUSIBLE / REJECTED.

| id | file:line | category | summary | status | verifier note |
|----|-----------|----------|---------|--------|---------------|
| C1 | crates/gatewayd/src/api/invoices.rs:381 | correctness | restore_pending runs on AlreadyAttached/IdempotencyConflict, retagging the winner's live attachment to pending | CONFIRMED | invoices.rs:374-388 runs restore_pending for every Err from insert_issued before matching the variant at 390-399; AttachmentAlreadyAttached (attachments.rs:308-309) retags the winner's object/version to payday-upload=pending, which main.tf:475-494 expires after 7 days; no test covers a loser whose winner bound the attachment. |
| C2 | crates/gatewayd/src/api/invoices.rs:728 | correctness | Party emails validated only for length/control chars, unlike expected_email | REJECTED | Documented contract: migration 0011:43-44 and product-plan §4.1 say party fields are bounded free text rendered verbatim, never parsed; party.email is never consumed programmatically. |
| C3 | crates/gateway-db/src/invoices.rs:163 | correctness | Invoice::try_from requires attribution columns that migration 0011 leaves NULL for legacy rows | PLAUSIBLE | Mechanically true (invoices.rs:163-181 errors on NULL; migration has no backfill; indexer.rs:855-858 maps to Configuration which requires_halt), but migration header says no production data / pre-release reset; breaks only if deployed over an un-reset main-era DB (e.g. sandbox). |
| P1 | crates/gatewayd/src/api/payer_verification.rs:192 | correctness | Pending email attempt (and 60s cooldown) recorded before send_code; provider failure locks payer out | CONFIRMED | begin_email_verification commits the pending row (verifications.rs:236-239) before send_code at payer_verification.rs:192; send failure has no rollback; cooldown uses max(created_at) regardless of status, and has_pending_email_verification returns true so confirm proceeds with no code sent. |
| P2 | crates/gatewayd/src/api/payer_verification.rs:171 | correctness | New payer session persisted before cooldown check; 429 leaves orphan session rows | CONFIRMED | With no/invalid bearer a new payer_sessions row is inserted at :169-174 before the cooldown check at :176-189; 429 path returns without deleting; no rate limiter on the route. |
| P3 | crates/gatewayd/src/api/payer_verification.rs:236 | correctness | OTP consumed at Auth0 before approve_email persists; DB failure spends code with no record | CONFIRMED | confirm_code at :234 consumes the OTP; approve_email tx begins at :236-244 and propagates DB errors with no compensation; re-submit -> otp_invalid, re-send blocked by per-invoice cooldown. |
| P4 | crates/gatewayd/src/api/payer_verification.rs:128 | correctness | Paid payer on gated invoice cannot re-unlock receipt after session lapses (gated_invoice 410 on closed invoice) | CONFIRMED | gated_invoice (:124-130) 410s unless Created/Funded/Deploying; session TTL 24h (verifications.rs:24); payer_response strips settlement_tx_hash and invoice fields when locked with no terminal carve-out; main returned settlement_tx_hash unconditionally; product-plan never addresses session expiry after settlement. |
| P5 | crates/gateway-db/src/verifications.rs:301 | correctness | approve_email stamps verification_completed_at with no status/deadline guard (identity path has one) | CONFIRMED | approve_email UPDATE (verifications.rs:301-308) has no status/expiration predicate; complete_invoice (identity.rs:1125-1136) does; trigger at 0011:514-516 emits verification.approved on any NULL->non-NULL transition. |
| R1 | crates/gateway-core/src/proof.rs:263 | correctness | verify_proof never checks attestation result; a signed 'pending' attestation verifies as passed | CONFIRMED | verify_proof (proof.rs:245-289) never reads payload.result/verified_at; ProofVerification has no result field; CLI proof verify prints ATTESTATION Passed with signer only (gateway-cli proof.rs:153-163); result shown only by proof download. |
| R2 | crates/gateway-cli/src/proof.rs:389 | correctness | confirm_receipts lets one on-chain Transfer satisfy duplicated proof entries; coverage inflated | CONFIRMED | Transfers are unsigned (proof.rs:293-295); coverage loop (300-314) sums without dedup; confirm_receipts dedups only hashes (cli proof.rs:369-373) then any() per entry (280-288). With --rpc-url confirm_settlement still requires the full outbound amount; offline the duplicated proof passes every check. |
| I1 | crates/gatewayd/src/identity/didit.rs:246 | correctness | Any warning containing 'MISMATCH' is treated as a failed name match -> Declined on an Approved session | CONFIRMED | didit.rs:246 bare substring risk.contains("MISMATCH") over every warning with no log_type filter (DiditWarning only has risk); reconciler.rs:155-159 turns provider Approved into Declined in matched mode. |
| I2 | crates/gatewayd/src/identity/reconciler.rs:100 | correctness | Provider Rejected (404) treated as outage; in_review attempt polled forever, payer permanently blocked | CONFIRMED | didit.rs:459-462 maps 404 to Rejected; reconciler.rs:100-111 treats Rejected like Unavailable -> defer_after_failure re-arms forever; nothing expires stale attempts; in_review blocks begin/request_review/decide_review. Scope: stuck in_review rows only (pending rows are abandoned on restart). |
| I3 | crates/gatewayd/src/identity/reconciler.rs:155 | correctness | Provider-level Approved recorded as Declined when any feature node is Declined (manual override lost) | CONFIRMED | reconciler.rs:148-159: provider Approved with any feature node Declined -> IdentityStatus::Declined; feature_status returns Declined if any node in the array is Declined; no precedence for provider-level Approved. |
| I4 | crates/gateway-db/src/identity.rs:423 | correctness | begin_identity_verification abandons a pending attempt whose Didit session already completed but was not yet polled | CONFIRMED | begin refuses only on review_required/in_review then abandons every pending attempt (identity.rs:421-430); identity_start_available stays true while pending so checkout offers restart during the 15s poll window; apply_decision/claim_due ignore abandoned rows. provider_reference collision not reachable. |
| I5 | crates/gateway-db/src/identity.rs:467 | correctness | Terminal status at session creation bypasses approve()/decline paths | PLAUSIBLE | create_session parses status from the create response (didit.rs:87-91,404-408); record_provider_session writes it straight and only arms polling when open, so an Approved/Declined at creation bypasses approve()/decline paths. Didit documents Not Started on create, so unlikely in practice. |
| I6 | crates/gatewayd/src/api/identity.rs:137 | altitude | Merchant verification detail re-derives facts from attempt rows with string literals; diverges from DbPayerSession::facts() | REJECTED | Premise wrong: reuse_credential (identity.rs:501-556) and decide_review both INSERT an approved attempt row with fact columns, so attempt-derived and session-derived facts agree; string-literal parsing is style only. |
| F1 | infra/main.tf:637 | correctness | Task role lacks s3:PutObjectVersionTagging though tagging calls pass versionId -> every attachment issuance AccessDenied in AWS | CONFIRMED | main.tf:637 grants PutObjectTagging but not PutObjectVersionTagging on a versioned bucket (:459); attachments.rs:283-286 sets version_id from HeadObject (:482/511), so AWS evaluates PutObjectVersionTagging -> AccessDenied. |
| F2 | infra/main.tf:680 | correctness | identity_environment/identity_secrets locals never wired into gatewayd task definition | CONFIRMED | local.identity_environment/identity_secrets appear only at their definitions (main.tf:48,:54); task definition concatenates only dashboard/payer env (:680) and payer_secrets (:688). |
| F3 | scripts/local-runner.sh:136 | correctness | PAYDAY_PUBLIC_BASE_URL defaults to API URL while checkout origin defaults to :3002; CORS/redirect mismatch | PLAUSIBLE | Mechanism holds (routes.rs:37 CORS from origin_header; payer.rs:369 self-redirect; dashboard calls API cross-origin), but PUBLIC_BASE_URL default is unchanged from main; the branch introduced the inconsistent HOSTED_CHECKOUT_ORIGIN=3002 default at :153. |
| F4 | crates/dev-identity/src/main.rs:197 | correctness | OTP store keyed by email only; code from payer client exchangeable for dashboard-audience JWT (dev only) | CONFIRMED | dev-identity main.rs:177-180 keys OTP by email only; token() (:189-203) never compares requesting client_id; loopback-only bind so dev-only. |
| X1 | crates/gateway-indexer/src/chain.rs:793 | correctness | Two Recovered events in the deployment tx -> permanent Transient -> sweep pipeline halts | CONFIRMED | Payment.recover() is external with no guard and emits Recovered; constructor also emits Recovered; two logs -> Transient (chain.rs:787-792); Transient is not requires_halt, open_sweep_batch returns the same batch each tick, apply_receipt re-runs classify with no skip path -> chain's sweep never finalizes. |
| X2 | crates/gateway-indexer/src/indexer.rs:593 | correctness | classify() searches settlement logs from first_observed_block; earlier zero-balance deployment yields permanent Transient | REJECTED | Search range ends at the sweep tx's own block, and the sweep's recover() emits Recovered for the late balance, so logs are non-empty. Real (different) consequence: settlement_tx_hash/block point at the sweep tx instead of the earlier deployment. |
| X3 | crates/gateway-db/src/sweeps.rs:767 | correctness | sweep_queue_stats counts gated-unverified invoices the claim query excludes -> false 'degraded' status | CONFIRMED | claim_sweep_batch (sweeps.rs:262-282) requires permissionless OR verification_completed_at; sweep_queue_stats (767-780) has no such filter; status.rs:80,127-131 flips degraded at 900s; indexer.rs:841-849 warns every tick. |
| W1 | web/components/checkout/attachment-link.tsx:45 | correctness | window.open with noopener returns null; component always shows 'blocked' state | CONFIRMED | attachment-link.tsx:44-45: window.open with noopener returns null in all major browsers; state always 'blocked'; tests mock open to return window. |
| W2 | web/components/dashboard/invoice-form.tsx:183 | correctness | Invoice can be issued while attachment upload/scan is in flight; PDF silently dropped | CONFIRMED | attachment-upload.tsx:40 onChange(null) at pick, id only after scan; only prop is onChange; invoice-form.tsx:410 submit disabled only by busy; :183 sends attachment?.id ?? null. |
| W3 | web/components/checkout/email-verification.tsx:50 | correctness | otp_resend_cooldown switches to code step without a session; payer bounces until cooldown lapses | CONFIRMED | email-verification.tsx:48-51 switches to code step regardless of payerSession; confirm (65-68) bounces without session; cooldown is per invoice (verifications.rs:201-209) and start returns no token on cooldown (payer_verification.rs:182-189). |
| W4 | web/components/checkout/checkout.tsx:38 | correctness | emailCodeSent never cleared after session invalidation; header contradicts control | CONFIRMED | Only write is checkout.tsx:69 setEmailCodeSent(true); never reset; checkout-state.ts:172-181 keeps email_pending after session invalidation. |
| W5 | web/components/checkout/identity-verification.tsx:67 | correctness | 'approved' status has no render branch; falls to disabled start button if parent refresh fails | PLAUSIBLE | No render branch for status approved (falls to default 199-217 with disabled button) but use-payment.ts re-polls with backoff after a failed tick, so the misleading state is transient. |
| W6 | web/lib/checkout-state.ts:158 | correctness | Locked payment with requirements.complete maps to verification_required; gate shows all facts Approved | CONFIRMED | payer.rs:267-277 with no session returns for_mode(mode, completed=true) = all facts Approved but content_unlocked=false; checkout-state.ts:157 falls to verification_required; verification-gate.tsx:67 lists all facts Approved under 'Verify to view this invoice'. |
| A1 | crates/gatewayd/src/api/auth.rs:366 | correctness | verify_session drops freshness/single-use checks and auto-provisions accounts | REJECTED | Documented intent (implementation-guide.md:1117): freshness/one-time-event enforcement kept only on API-key issuance; verify_session still checks iss/aud/exp/azp + email-OTP provenance; main's require_api_key accepted non-expiring keys, and auto-provisioning already existed in main's issue_api_key_inner. |
| S1 | crates/gateway-db/src/identity.rs:365 | simplification | begin_identity_verification re-pastes identity_state's two queries | CONFIRMED | identity.rs attempts query (330-348 vs 380-397) and reviews EXISTS (349-363 vs 398-411) byte-identical; only executor/bind form differ; helper generic over PgExecutor is viable. |
| S2 | crates/gatewayd/src/identity/mod.rs:45 | simplification | IdentityProviderStatus is a subset copy of gateway_db::IdentityStatus mapped 1:1 | PLAUSIBLE | Exact subset minus ReviewRequired and a pure 1:1 map, but the narrower type keeps vendor adapters from asserting ReviewRequired (Payday's own verdict), so it has a role. |
| S3 | crates/gatewayd/src/deployment.rs:22 | reuse | ExpectedDeployment/DeploymentError/check duplicated in gateway-indexer/src/deployment.rs; already diverging | CONFIRMED | ExpectedDeployment + three error variants with identical text + compares duplicated (gatewayd 22-71 vs indexer 16-51); already diverge (indexer skips chain_id); gateway-core has alloy-primitives+thiserror. |
| S4 | crates/gateway-db/src/invoices.rs:411 | simplification | same_issuance duplicates handler same_request; both re-derive what attribution_hash commits to | PLAUSIBLE | Both chains live (gateway-db invoices.rs:411-444; api/invoices.rs:673-695). Hash-based collapse is NOT equivalent (snapshot includes resolved expiration_timestamp; metadata/customer_id etc. outside snapshot); Rust-vs-Rust duplication is real. |
| S5 | crates/gateway-db/migrations/0011_verified_customer_funds.sql:353 | simplification | payer_credentials.status 'revoked'/'expired' never written by production code | CONFIRMED | Only production writes are two INSERTs with 'active' (identity.rs:1049,1065); 'revoked' only in a test; note partial unique indexes and ON CONFLICT reference status. |
| S6 | crates/gatewayd/src/api/payer_verification.rs:101 | altitude | retry_available has two unrelated definitions (payer vs merchant DTO) | CONFIRMED | payer_verification.rs:101 aliases identity_start_available; api/identity.rs:168-175 computes a different predicate; same wire name in two DTOs; SDK comment accurate for neither. |
| S7 | crates/gateway-db/src/identity.rs:440 | altitude | Provider 'didit' hardcoded in gateway-db INSERTs while webhook path takes provider param | CONFIRMED | 'didit' hardcoded at identity.rs:440 and 527 while record_webhook takes provider param; CHECK admits auth0/didit/manual. |
| S8 | crates/gateway-db/src/invoices.rs:978 | altitude | is_gated() re-expressed as SQL literals in five statements | PLAUSIBLE | Five literals exist but only the three 'permissionless' ones mirror is_gated(); is_gated lives in attribution.rs:158 and has several Rust callers; viable fix is binding as_str() values. |
| U1 | crates/gatewayd/src/api/payer.rs:233 | reuse | parse_invoice_id re-implements gateway_core::payment_id with looser Uuid::from_str | CONFIRMED | payer.rs:233-237 Uuid::from_str accepts simple/urn/braced/uppercase; gateway_core::payment_id (invoice.rs:27-32) enforces canonical form; branch made loose parser pub(crate) and added two callers. Body pre-dates branch; only observable as accept-vs-reject. |
| U2 | crates/gateway-core/src/attribution.rs:190 | reuse | valid_email copied from api/auth.rs (and CLI validate_email) | CONFIRMED | attribution.rs:190-199 and auth.rs:294-303 byte-for-byte identical; gateway-core re-exports attribution::*; CLI validate_email is a near-duplicate. |
| U3 | crates/gatewayd/src/api/payer_verification.rs:136 | reuse | Email normalization (.trim().to_lowercase()) triplicated | PLAUSIBLE | Three sites exist; expected_email() re-normalizes a value already normalized at issuance (redundant); no shared helper exists to name, so this is a new-helper suggestion. |
| U4 | crates/gatewayd/src/api/identity.rs:79 | reuse | fact() hand-parses VerificationFactStatus strings; enum owns as_str | CONFIRMED | api/identity.rs:79-86 hand-matches strings; :143/:203 compare raw strings; dto.rs VerificationFactStatus has as_str but no FromStr; gateway-db keeps a third table (identity.rs:55-65,77). |
| U5 | web/components/checkout/identity-verification.tsx:22 | reuse | Hand-rolled 4s poll loop duplicates web/lib/poll.ts; no backoff/hidden pause | CONFIRMED | poll.ts exports DEFAULT_POLL_MS/backoffMs/jitter; identity-verification.tsx imports none, fixed POLL_MS on success and failure (:75,:83), no hidden pause. |
| E1 | crates/gatewayd/src/identity/reconciler.rs:46 | efficiency | Claimed batch reconciled serially; 60s lease can expire mid-loop | CONFIRMED | BATCH=20, LEASE=60s (reconciler.rs:21-23), serial loop :46-51, 15s provider timeout -> batch can run ~300s past lease; re-claim is idempotent but wasted. |
| E2 | crates/gateway-db/migrations/0011_verified_customer_funds.sql:345 | efficiency | No index on payer_verifications.payer_session_id for OTP start/confirm queries | CONFIRMED | Only provider_reference, poll, invoice indexes exist (sql:339-346); verifications.rs:217,251,290 filter on payer_session_id; approve_email runs it in a write tx (holds payer_sessions row lock, not invoice FOR UPDATE). |
| E3 | crates/gatewayd/src/attestation.rs:57 | efficiency | aws_config::load_defaults called twice; startup fetches strictly sequential | CONFIRMED | main.rs:59 and attestation.rs:57 both call load_defaults; startup awaits Auth0Verifier::new, load_defaults, from_config, payer verifier sequentially. |
| E4 | crates/gatewayd/src/attachments.rs:490 | efficiency | inspect_and_hash buffers whole object (up to 5 MiB) for magic bytes + SHA-256 | CONFIRMED | attachments.rs:489-503 storage.get returns full Vec (cap 5 MiB) for starts_with(PDF_MAGIC) + Sha256::digest. |
| E5 | crates/gatewayd/src/api/proof.rs:29 | efficiency | get_proof re-queries invoice fields and KMS-signs a deterministic payload per GET | CONFIRMED | proof.rs:27-33 re-SELECTs six invoice columns already on DbInvoice (proofs.rs:62-64); payload has no issued_at/nonce so KMS sign per GET (proof.rs:78-97) is cacheable. |
| E6 | web/components/checkout/identity-verification.tsx:72 | efficiency | Fixed 4s poll during in_review with no backoff; hours of no-op load | CONFIRMED | POLL_MS=4_000 fixed at :72/:80 while pending/in_review, no backoff/visibility; each tick runs 4 DB queries (status handler). |
